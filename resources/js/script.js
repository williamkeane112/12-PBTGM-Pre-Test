const checkbox = document.getElementById("updateStatus");
const form = document.getElementById("UpdateForm");

document.addEventListener("updateStatus", function () {
    checkbox.addEventListener("change", function () {
        // form.submit();
        console.log("====================================");
        console.log("alsjka");
        console.log("====================================");
    });
});
